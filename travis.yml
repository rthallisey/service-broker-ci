language: go
go:
  - "1.8.x"
dist: trusty
sudo: required
group: edge

before_script:
  - bash <(curl -s https://raw.githubusercontent.com/openshift/service-broker-ci/master/setup-gate.sh)
  - go get github.com/rthallisey/service-broker-ci/tree/master/cmd/ci
script:
  - ci
